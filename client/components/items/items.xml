<div>
	<div>
		<button onClick={addItem}>Add New Item</button>
		<button onClick={addManyItems}>Add Many Items</button>
	</div>
	{{#items}}
		<Item color="{{color}}"
			  id="{{id}}"
			  onChange={handleItemChange}
			  onRemove={handleRemoveItem} />
	{{/items}}
</div>
